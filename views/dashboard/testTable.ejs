<!DOCTYPE html>
<html>
  <head>
    <!-- CSS Assets -->
    <link rel="stylesheet" href="../css/output.css" />
    <link rel="stylesheet" href="../css/app.css" />
    <link rel="stylesheet" href="../css/custom.css" />
    <link rel="stylesheet" href="../css/w3.css" />
    <link rel="stylesheet" href="../css/dataTables.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/gridjs/dist/theme/mermaid.min.css"
      rel="stylesheet"
    />
    <!-- Javascript Assets -->
    <script src="https://unpkg.com/doc@latest/dist/doc.min.js"></script>
    <script src="../js/app.js" defer></script>
    <script src="../js/jquery.js"></script>
    <script src="../js/cdn/dataTables.js"></script>
    <script src="../js/cdn/socket.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/gridjs/dist/gridjs.umd.js"></script>
  </head>
  <style>
    .highlight {
      background-color: #f0f9ff !important; /* Light blue color for highlighting */
    }
  </style>
  <body class="slabo">
    <main>
      <h1>Socket io Test</h1>
      <button id="sendMessageButton">Send Message</button>
      <div class="bg-white p-3">
        <h2>how are you</h2>
        <button
          id="getSelectedData"
          class="mt-4 bg-blue-500 text-white py-2 px-4 rounded"
        >
          Get Selected Data
        </button>
        <div class="flex notifications justify-start items-start">
          <div class="p-2 bg-slate-300 bg-opacity-30" onclick="showSuccess()">
            success
          </div>
          <div class="p-2 bg-slate-300 bg-opacity-30" onclick="error()">
            error
          </div>
          <div
            class="p-2 bg-slate-300 bg-opacity-30"
            onclick="showSuccess_mid()"
          >
            success-mid
          </div>
          <div
            class="p-2 bg-slate-300 bg-opacity-30"
            onclick="showSuccess_range()"
          >
            success-range
          </div>
        </div>
        <div class="is-scrollbar-hidden min-w-full overflow-x-auto">
          <table
            id="examples"
            class="is- hover w-full text-left"
            style="width: 100%"
          >
            <thead>
              <tr>
                <th class="py-2 px-4 bg-gray-200">
                  <input
                    id="selectAllCheckbox"
                    class="form-checkbox is-basic bg-slate-100 h-4 w-4 rounded border-slate-400/70 checked:bg-primary checked:border-primary hover:border-primary focus:border-primary dark:border-navy-400 dark:checked:bg-accent dark:checked:border-accent dark:hover:border-accent dark:focus:border-accent"
                    type="checkbox"
                  />
                </th>
                <th>Name</th>
                <th>Position</th>
                <th>Office</th>
                <th>Age</th>
                <th>Start date</th>
                <th>Salary</th>
              </tr>
            </thead>

            <tbody>
              <tr>
                <td class="border px-4 py-2">
                  <input
                    class="row-checkbox form-checkbox is-basic h-4 w-4 rounded border-slate-400/70 checked:bg-primary checked:border-primary hover:border-primary focus:border-primary dark:border-navy-400 dark:checked:bg-accent dark:checked:border-accent dark:hover:border-accent dark:focus:border-accent"
                    type="checkbox"
                  />
                </td>
                <td>Tiger Nixon</td>
                <td>System Architect</td>
                <td>Edinburgh</td>
                <td>61</td>
                <td>$320,800</td>
              </tr>
              <tr>
                <td class="border px-4 py-2">
                  <input type="checkbox" class="row-checkbox" />
                </td>
                <td>Garrett Winters</td>
                <td>Accountant</td>
                <td>Tokyo</td>
                <td>63</td>
                <td>$170,750</td>
              </tr>
              <tr>
                <td class="border px-4 py-2">
                  <input type="checkbox" class="row-checkbox" />
                </td>
                <td>Ashton Cox</td>
                <td>Junior Technical Author</td>
                <td>San Francisco</td>
                <td>66</td>
                <td>$86,000</td>
              </tr>
            </tbody>
            <!-- <tfoot>
              <tr>
                <th>Name</th>
                <th>Position</th>
                <th>Office</th>
                <th>Age</th>
                <th>Start date</th>
                <th>Salary</th>
              </tr>
            </tfoot> -->
          </table>
        </div>
      </div>
    </main>
  </body>
</html>
<script>
  function showSuccess() {
    showNoti(
      "success",
      "Your form request has been submitted successfully",
      5000
    );
  }

  function showSuccess() {
    showNoti(
      "success",
      "Your form request has been submitted successfully",
      5000
    );
  }

  function showSuccess() {
    showNoti(
      "success",
      "Your form request has been submitted successfully",
      5000
    );
  }

  function showSuccess() {
    showNoti(
      "success",
      "Your form request has been submitted successfully",
      5000
    );
  }
</script>
<script>
  $("#examples").DataTable({
    info: false,
    ordering: false,
    paging: false,
  });
</script>

<script>
  // Get the "Select All" checkbox and all row checkboxes
  const selectAllCheckbox = document.getElementById("selectAllCheckbox");
  const rowCheckboxes = document.querySelectorAll(".row-checkbox");
  const tbody = document.querySelector("tbody");

  // Add event listener to "Select All" checkbox
  selectAllCheckbox.addEventListener("change", function () {
    rowCheckboxes.forEach((checkbox) => {
      checkbox.checked = this.checked;
      toggleHighlight(checkbox.closest("tr"), this.checked);
    });
  });

  // Add event listener to each row checkbox
  rowCheckboxes.forEach((checkbox) => {
    checkbox.addEventListener("change", function () {
      toggleHighlight(this.closest("tr"), this.checked);
    });
  });

  // Function to toggle the highlight class
  function toggleHighlight(row, highlight) {
    if (highlight) {
      row.classList.add("highlight");
    } else {
      row.classList.remove("highlight");
    }
  }

  // Add event listener to "Get Selected Data" button
  document
    .getElementById("getSelectedData")
    .addEventListener("click", function () {
      const selectedData = [];
      rowCheckboxes.forEach((checkbox) => {
        if (checkbox.checked) {
          const row = checkbox.closest("tr");
          const rowData = {
            name: row.cells[1].innerText,
            position: row.cells[2].innerText,
            office: row.cells[3].innerText,
            age: row.cells[4].innerText,
            startDate: row.cells[5].innerText,
          };
          selectedData.push(rowData);
        }
      });

      console.log(selectedData);
    });
</script>

<script>
  // Connect to the WebSocket server
  const socket = io("http://localhost:5000");

  // Connection opened
  socket.on("connect", () => {
    console.log("Connected to WebSocket server");
  });

  // Listen for messages
  socket.on("message", (data) => {
    console.log("Message from server:", data);
  });

  // Handle connection close
  socket.on("disconnect", () => {
    console.log("WebSocket connection closed");
  });

  // Handle errors
  socket.on("error", (error) => {
    console.error("WebSocket error:", error);
  });

  // Send a message to the server
  document.getElementById("sendMessageButton").addEventListener("click", () => {
    socket.send("Hello from the frontend!");
  });
</script>
